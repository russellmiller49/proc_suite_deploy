{% set _render_style = (render_style | default("", true)) | lower %}
{% set _method = (proc.confirmation_method or "imaging") | trim %}
{% set _pattern = (proc.rebus_pattern or "") | trim %}

{% if _pattern %}rEBUS demonstrated {{ _pattern | lower }} pattern.{% endif %}
Tool-in-lesion was confirmed with {{ _method }}.
{% if proc.screenshots_saved %}Screenshots were saved to the record.{% endif %}
{% if proc.notes %}Notes: {{ proc.notes }}.{% endif %}
