With an existing chest tube or IPC connected to a drainage device, sequential balloon occlusion of {{ proc.culprit_segment }} was performed{% if proc.balloon_type or proc.balloon_size_mm %} using{% if proc.balloon_type %} {{ proc.balloon_type }}{% endif %}{% if proc.balloon_size_mm %} {{ proc.balloon_size_mm }} mm{% endif %} balloon{% endif %} while monitoring for cessation of bubbling/flow.
Leak reduction with occlusion was{% if proc.leak_reduction %} {{ proc.leak_reduction }}{% else %} assessed{% endif %}.{% if proc.methylene_blue_used or proc.contrast_used %} Confirmation with instillation of{% if proc.methylene_blue_used %} methylene blue{% endif %}{% if proc.contrast_used %}{% if proc.methylene_blue_used %} and{% endif %} dilute contrast{% endif %} through the chest tube was performed{% if proc.instillation_findings %} showing {{ proc.instillation_findings }}{% endif %}.{% endif %}
{% if proc.notes %}Notes: {{ proc.notes }}.{% endif %}
Based on localization, valve placement or sealant application was planned as indicated. The patient tolerated the procedure well.
