global:
  scrape_interval: 15s
  scrape_timeout: 10s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "procedure_suite"
    static_configs:
      - targets:
          - "localhost:8000"
    metrics_path: /metrics
    scheme: http

  # Uncomment these only if you later run separate services:
  # - job_name: "procedure_suite_coder"
  #   static_configs:
  #     - targets: ["localhost:8001"]
  #   metrics_path: /metrics
  #   scheme: http
  #
  # - job_name: "procedure_suite_reporter"
  #   static_configs:
  #     - targets: ["localhost:8002"]
  #   metrics_path: /metrics
  #   scheme: http
