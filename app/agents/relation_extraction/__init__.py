"""Relation extraction (Phase 8).

This package provides shadow-mode relation extraction plumbing:
- deterministic heuristic relations (baseline)
- optional ML relations (future / experimental)
- merge strategy that prefers ML when confidence is high
"""

